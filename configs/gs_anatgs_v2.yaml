name: gs_anatgs_v2

# Keep strong strict-baseline behavior; add only anatomy regularization.
anatomy_init: false
organ_aware_prune: false

lambda_region: 0.01
region_vol_size: 64
region_loss_interval: 1
region_organs: "2,3,4,5,6,7,8,9"
